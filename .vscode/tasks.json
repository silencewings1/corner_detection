{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake build",
            "command": "cmake .. & make -j8",
            "options": {
                "cwd": "${workspaceFolder}/build/"
            }
        }
    ],
    "version": "2.0.0"
}