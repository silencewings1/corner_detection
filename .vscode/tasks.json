{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake build",
            "command": "cmake .. & make",
            "options": {
                "cwd": "${workspaceFolder}/build/"
            }
        }
    ],
    "version": "2.0.0"
}